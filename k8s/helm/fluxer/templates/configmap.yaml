apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fluxer.fullname" . }}
  labels:
    {{- include "fluxer.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
