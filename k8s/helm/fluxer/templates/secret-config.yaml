{{- if .Values.fluxerConfig.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "fluxer.fullname" . }}-config
  labels:
    {{- include "fluxer.labels" . | nindent 4 }}
type: Opaque
stringData:
  config.json: |
{{ .Values.fluxerConfig.json | indent 4 }}
{{- end }}
